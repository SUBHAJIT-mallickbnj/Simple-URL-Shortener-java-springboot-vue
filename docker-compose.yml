services:
  mongodb:
    image: mongo:7
    container_name: linknest-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - linknest-data:/data/db

volumes:
  linknest-data: